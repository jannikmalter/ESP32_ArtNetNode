# R22 — Native Art-Net output configuration and query

**Requirement.** The node shall support native Art-Net output configuration and
query: set the per-output universe patch and node name from a console via
ArtAddress (`0x6000`), and report current config in ArtPollReply.

## Rationale
Replaces the current `0x6000`/`0x7000` ArtPoll-trigger quirk
([src/main.c:724](src/main.c#L724)), so standard Art-Net controllers can configure
the node without the raw TCP CLI. Traces to **G1**. Touches B12 (node name).

## Acceptance criteria
Done in `reqs.md` (2026-06-26).

- [x] ArtAddress (`0x6000`) sets the per-output universe patch and persists it (NVS) — `handle_artaddress()`.
- [x] ArtAddress can set the node name and persists it (NVS keys `SHORTNAME`/`LONGNAME`).
- [x] ArtPollReply reports current per-port universes, name, and status — one bound port per output.
- [ ] ~~Optional: ArtInput (`0x7000`) handled~~ — out of scope: node is output-only.
- [x] The old `0x6000`/`0x7000` ArtPoll-trigger quirk is removed.

## Implementation notes (as-built)
- **Per-port binding (Art-Net 4).** A 15-bit Port-Address = `NetSwitch(7) :
  SubSwitch(4) : Sw(4)`. A single ArtPollReply/ArtAddress carries one Net + one
  Sub but four Sw nibbles, so the legacy scheme could only describe 4 ports in one
  block of 16. Art-Net 4 encodes one universe per ArtPollReply (`NumPortsLo=1`),
  differentiated by `BindIndex` (1..N) + `BindIp` ([docs/art-net.pdf](../docs/art-net.pdf)
  p.149, p.4486-4489). The node therefore sends 7 ArtPollReply packets (BindIndex
  1..7), each advertising one output's fully independent universe.
- **ArtAddress.** BindIndex selects the output (`idx = bind<=1 ? 0 : bind-1`). The
  new universe is rebuilt from the current one, replacing only the Net/Sub/Sw
  sub-fields whose source byte has bit7 set (spec: "ignored unless bit 7 is high"),
  so a partial program never zeroes a universe. PortName/LongName are applied if
  their first byte is non-null. Persisted via the existing
  `stopDMX()`/`startDMX()` + NVS handshake (`save_dmx_patch`/`save_node_name`).
- **Node name (closes B12).** `node_short_name`/`node_long_name` seed from
  `VARIANT_NAME`, load from NVS, and are reported in ArtPollReply + `/api/state`.
- **Sync/num_chan reset on Art-Net patch change.** Sync mode and channel count
  cannot be set over Art-Net, so any ArtAddress patch change resets them to
  defaults (free-run, 512 channels) — avoids a confusing hidden state for users
  who configure the node only via Art-Net.

## Links
Related bugs: B12 · Todo: T7
